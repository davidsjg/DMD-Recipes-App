import React from "react";

export default function QueryFunction() {
  {
    if (recipeSearch.current.value === "") {
      console.log("it is undefined and should be blank");
    } else {
      console.log("it is not null and is: " + recipeSearch);
    }
  }

  return <></>;
}
